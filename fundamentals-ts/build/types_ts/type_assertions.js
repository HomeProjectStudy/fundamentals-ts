"use strict";
// Afrimar algum tipo
let statusAtual = 1;
let changeStatus = 0;
// Afrimando que o status atual ẽ um numero
changeStatus = statusAtual;
changeStatus = statusAtual; // dessa forma tambẽm ẽ aceito.
console.log(changeStatus);
